# URLShortenerAPI

This is a URL Shortener API built with ASP.NET Core. It provides endpoints for user authentication, creating shortened URLs, and redirecting users from short links to their original URLs. The system uses JWT-based authentication to secure user-specific operations and stores data in a relational database. The API is designed to be consumed by a frontend application and is currently deployed on Render, with a React frontend hosted separately on AWS.
